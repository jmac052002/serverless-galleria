{
  "name": "gallery",
  "version": "1.0.0",
  "description": "Gallery API - returns pre-signed URLs for authenticated users",
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('bundle', {recursive:true})\" && esbuild src/index.js --bundle --platform=node --target=node22 --outfile=bundle/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0"
  }
}
